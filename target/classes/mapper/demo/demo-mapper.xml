<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo">
	<insert id="insertDev" parameterType="dev">   <!-- parameterType 생략해도 됨 -->
		insert into
			dev
		values(
			seq_dev_no.nextval,
			#{name},
			#{career},
			#{email},
			#{gender},
			<!-- 
			<foreach 
				collection="lang"
				item="l"
				open="'"
				close="'"
				separator=", ">
				${l}   
							어떻게 해도 sqlException : invalid column index 떠서 시간을 엄청 날렸는데 알수가 없어서 답을 봤다. 여기에 #{l} 가 아니라 ${l} 이라고 적혀있었다.
							매퍼에 ${} 이게 대체 뭔가 해서 검색해보니까 ${} 이걸로 하면 값을 그대로 출력하고 #{} 이걸로 하면 문자열로 처리해서 양 끝에 따옴표가 붙는다고 한다.
							이 insert문에서는 'Java, Python' 이런 식으로 적어야 하므로 따옴표가 붙으면 안된다. 그래서 ${}로 해야 하는 거였다.
							답 안 찾아봤으면 절대 못풀었겠네
			</foreach>
			-->
			#{lang}
		)
	</insert>
	
	<select id="selectDevList" resultType="dev">
		select
			*
		from
			dev
		order by
			no desc
	</select>
</mapper>